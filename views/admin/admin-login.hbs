<div class='login-form'>
  <h1>Вход в админку</h1>

  <form action='/admin/login' method='POST'>
    <div class='form-group'>
      <label>Логин:</label>
      <input type='text' name='username' required />
    </div>

    <div class='form-group'>
      <label>Пароль:</label>
      <input type='password' name='password' required />
    </div>

    <button type='submit'>Войти</button>
    {{#if error}}
      <div class='error'>{{error}}</div>
    {{/if}}
  </form>
</div>

<style>
  .login-form { max-width: 400px; margin: 50px auto; padding: 20px; background: #fff; border-radius:
  5px; box-shadow: 0 0 10px rgba(0,0,0,0.1); } .form-group { margin-bottom: 15px; } label { display:
  block; margin-bottom: 5px; } /* Исправленный input */ input { width: 100%; padding: 8px; /*
  Равномерные отступы со всех сторон */ border: 1px solid #ddd; border-radius: 4px; box-sizing:
  border-box; /* Важно! Чтобы padding не увеличивал ширину */ } button { background: #2c3e50; color:
  white; padding: 10px 15px; border: none; border-radius: 4px; cursor: pointer; width: 100%; /* Для
  единообразия */ } .error { color: #e74c3c; margin: 15px 0; text-align: center; }
</style>